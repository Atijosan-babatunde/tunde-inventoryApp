{
    "projects": {
      "default": "apps/angular-dashboard"
    },
    "builds": [
      {
        "src": "apps/angular-dashboard/package.json",
        "use": "@vercel/static-build",
        "config": { "distDir": "dist/angular-dashboard" }
      }
    ]
  }